import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    container: {
      flex: 1,
      marginTop: 20,
      backgroundColor: '#FFF'
    },

    separator: {
      flex: 1,
      height: 1.25,
      backgroundColor: '#DDD'
    }
  });
