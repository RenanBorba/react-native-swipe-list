import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  container: {
    backgroundColor: '#FFF',
    paddingHorizontal: 10,
    paddingVertical: 20
  },

  text: {
    fontSize: 17,
    color: '#666'
  },

  leftAction: {
    flex: 1,
    backgroundColor: '#399E4D',
    justifyContent: 'center'
  },

  rightAction: {
    backgroundColor: '#FF0430',
    justifyContent: 'center'
  },

  textAction: {
    fontSize: 17,
    color: '#FAFAFA',
    padding: 20
  }
});
